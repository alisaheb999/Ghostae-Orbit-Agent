<UserControl x:Class="Ghostae.Orbit.Agent.Views.DesktopView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="900">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,28,32,32">

            <!-- Header -->
            <Grid Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Desktop Telemetry" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock Text="Hardware utilization and system environment" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <Button Content="Refresh Metrics" Style="{StaticResource OrbitSecondaryButton}" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>

            <!-- Metrics Cards Grid -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- CPU Card -->
                <Border Grid.Column="0" Style="{StaticResource OrbitCard}">
                    <StackPanel>
                        <TextBlock Text="CPU LOAD" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding CpuFormatted}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentRedBrush}" Margin="0,8,0,8"/>
                        <ProgressBar Style="{StaticResource OrbitProgressBar}" Value="{Binding CpuUsage}" Maximum="100"/>
                    </StackPanel>
                </Border>

                <!-- RAM Card -->
                <Border Grid.Column="2" Style="{StaticResource OrbitCard}">
                    <StackPanel>
                        <TextBlock Text="MEMORY" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding RamFormatted}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,12,0,12"/>
                        <ProgressBar Style="{StaticResource OrbitProgressBar}" Value="{Binding RamUsage}" Maximum="100"/>
                    </StackPanel>
                </Border>

                <!-- System Info Card -->
                <Border Grid.Column="4" Style="{StaticResource OrbitCard}">
                    <StackPanel>
                        <TextBlock Text="UPTIME" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock Text="{Binding UptimeFormatted}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,10,0,4"/>
                        <TextBlock Text="{Binding MachineName}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- OS Info Card -->
            <Border Style="{StaticResource OrbitCard}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Path Data="{StaticResource IconDesktop}" Fill="{StaticResource TextPrimaryBrush}" Width="24" Height="24" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Operating System" FontWeight="SemiBold" FontSize="15" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock Text="{Binding OsName}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Storage Drives List Card -->
            <Border Style="{StaticResource OrbitCard}">
                <StackPanel>
                    <TextBlock Text="Storage Volumes" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>

                    <ItemsControl ItemsSource="{Binding Drives}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderLightBrush}" Padding="0,12">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>

                                        <Path Data="{StaticResource IconFiles}" Fill="{StaticResource TextSecondaryBrush}" Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center"/>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding Label}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="8,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <ProgressBar Grid.Column="2" Style="{StaticResource OrbitProgressBar}" Value="{Binding FreePercentage}" Maximum="100" VerticalAlignment="Center" Margin="0,0,16,0"/>

                                        <TextBlock Grid.Column="3" Text="{Binding FreeFormatted}" FontSize="12" FontWeight="Medium" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
