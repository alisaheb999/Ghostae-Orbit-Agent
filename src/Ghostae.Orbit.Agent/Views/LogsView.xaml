<UserControl x:Class="Ghostae.Orbit.Agent.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="900">
    <Grid Margin="32,28,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="System Logs" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Structured operational audit trail and communication diagnostics" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Clear Logs" Style="{StaticResource OrbitSecondaryButton}" Command="{Binding ClearLogsCommand}" Margin="0,0,8,0" Padding="12,6"/>
                <Button Content="Export Logs" Style="{StaticResource OrbitPrimaryButton}" Command="{Binding ExportLogsCommand}" Padding="12,6"/>
            </StackPanel>
        </Grid>

        <!-- Category Filters -->
        <Border Grid.Row="1" Style="{StaticResource OrbitCardCompact}" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Category: " FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                <RadioButton Content="All" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="All" IsChecked="True" Margin="0,0,8,0"/>
                <RadioButton Content="Connection" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="Connection" Margin="0,0,8,0"/>
                <RadioButton Content="Tasks" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="Tasks" Margin="0,0,8,0"/>
                <RadioButton Content="Adobe" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="Adobe" Margin="0,0,8,0"/>
                <RadioButton Content="Files" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="Files" Margin="0,0,8,0"/>
                <RadioButton Content="Recording" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="Recording" Margin="0,0,8,0"/>
                <RadioButton Content="System" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="System" Margin="0,0,8,0"/>
                <RadioButton Content="Errors" Style="{StaticResource OrbitNavButton}" Command="{Binding FilterCategoryCommand}" CommandParameter="Errors"/>
            </StackPanel>
        </Border>

        <!-- Logs Viewer List -->
        <Border Grid.Row="2" Style="{StaticResource OrbitCard}" Padding="12">
            <ListBox ItemsSource="{Binding DisplayLogs}" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ScrollViewer.CanContentScroll="True">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0,0,0,1" BorderBrush="{StaticResource BorderLightBrush}" Padding="4,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="75"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding TimeFormatted}" FontSize="12" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>

                                <Border Grid.Column="1" CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center" Background="{StaticResource BgSubtleBrush}">
                                    <TextBlock Text="{Binding Level}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </Border>

                                <TextBlock Grid.Column="2" Text="{Binding Category}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource AccentRedBrush}" VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Message}" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Details}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,0,0"
                                               Visibility="{Binding Details, Converter={x:Null}}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

    </Grid>
</UserControl>
