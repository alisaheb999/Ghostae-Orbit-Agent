<UserControl x:Class="Ghostae.Orbit.Agent.Views.FilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="900">
    <Grid Margin="32,28,32,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="File Manager" FontSize="26" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Controlled file operations for Ghostae Orbit Agent tasks" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Path Bar & Action Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource OrbitCardCompact}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Content="↑ Up" Style="{StaticResource OrbitSecondaryButton}" Command="{Binding NavigateUpCommand}" Margin="0,0,8,0" Padding="10,6" FontSize="12"/>

                <TextBox Grid.Column="1" Text="{Binding CurrentPath, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource OrbitTextBox}" Margin="0,0,12,0"/>

                <TextBox Grid.Column="2" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource OrbitTextBox}" Margin="0,0,12,0"/>

                <StackPanel Grid.Column="3" Orientation="Horizontal">
                    <Button Content="Search" Style="{StaticResource OrbitSecondaryButton}" Command="{Binding SearchCommand}" Margin="0,0,8,0" Padding="12,6" FontSize="12"/>
                    <Button Content="New Folder" Style="{StaticResource OrbitPrimaryButton}" Command="{Binding CreateFolderCommand}" Padding="12,6" FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- File List Table -->
        <Border Grid.Row="2" Style="{StaticResource OrbitCard}" Padding="0">
            <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource BorderLightBrush}"
                      Background="Transparent" BorderThickness="0" RowHeight="38" FontSize="13"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                    <DataGridTextColumn Header="Size" Binding="{Binding SizeFormatted}" Width="110"/>
                    <DataGridTextColumn Header="Modified" Binding="{Binding ModifiedFormatted}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
